// import { createStoreModule } from '@/store/template'

// export default commitRootInModule => createStoreModule({ state: { visibleOfPreview: false } })

export default commitRootInModule => {
  // 该页面已合并到数据采集/模板管理
}
